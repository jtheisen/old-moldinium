image: Visual Studio 2019

configuration: Debug


before_build:
- nuget restore

build:
  verbosity: minimal

after_build:
- ps: nuget pack Moldinium\Moldinium.csproj

artifacts:
- path: '*.nupkg'
  name: nuget-package
